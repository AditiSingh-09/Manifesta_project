REACT_APP_API_URL=http://localhost:5000/api    # API base URL for your backend (change port if needed)

# JWT Secret (for signing tokens)
JWT_SECRET="itcouldbeanything? "            # Change this to your secret key

# Database credentials (for backend)
DB_HOST="localhost"
DB_PORT=5432
DB_NAME="manifesta"
DB_USER="postgres"
DB_PASSWORD="sky"
